_format_version: "3.0"
_konnect:
  control_plane_name: kong-local-dev

services:
- name: tmn-mock-service
  host: example.com
  protocol: https
  port: 443
  enabled: true
  connect_timeout: 60000
  read_timeout: 60000
  write_timeout: 60000
  retries: 5

  routes:
  # -------- Route 1: /mock (Employee) --------
  - name: get-monthly-report
    tags: [managed-by-deck]
    paths:
      - /mock
    methods: [GET, POST, PUT, PATCH]
    protocols: [https]
    strip_path: true
    preserve_host: false

    plugins:
    - name: mocking
      enabled: true
      protocols: [http, https]
      config:
        api_specification: |+
          openapi: 3.0.3
          info:
            title: Employee Mock API
            version: "1.0.0"
          paths:
            /mock:
              get:
                responses:
                  "200":
                    description: Random employee
                    content:
                      application/json:
                        schema:
                          type: object
                          properties:
                            firstName: { type: string }
                            middleName: { type: string }
                            birthDate: { type: string, format: date }
                            empId: { type: string }

  # -------- Route 2: /customer --------
  - name: customer-route
    tags: [managed-by-deck]
    paths: [/customer]
    methods: [GET]
    protocols: [https]
    strip_path: true
    preserve_host: false
    plugins:
    - name: mocking
      enabled: true
      protocols: [http, https]
      config:
        api_specification: |+
          openapi: 3.0.3
          info:
            title: Customer Mock API
            version: "1.0.0"
          paths:
            /customer:
              get:
                responses:
                  "200":
                    description: Random customer
                    content:
                      application/json:
                        schema:
                          type: object
                          properties:
                            customerName: { type: string }
                            customerId: { type: string }
                            customerAcc: { type: string }

  # -------- Route 3: /accounts --------
  - name: accounts-route
    tags: [managed-by-deck]
    paths: [/accounts]
    methods: [GET]
    protocols: [https]
    strip_path: true
    preserve_host: false
    plugins:
    - name: mocking
      enabled: true
      protocols: [http, https]
      config:
        api_specification: |+
          openapi: 3.0.3
          info:
            title: Accounts API
            version: "1.0.0"
          paths:
            /accounts:
              get:
                responses:
                  "200":
                    description: Account list
                    content:
                      application/json:
                        schema:
                          type: array
                          items:
                            type: object
                            properties:
                              accountId: { type: string }
                              type: { type: string }
                              balance: { type: number }

  # -------- Route 4: /balance --------
  - name: balance-route
    tags: [managed-by-deck]
    paths: [/balance]
    methods: [GET]
    protocols: [https]
    strip_path: true
    preserve_host: false
    plugins:
    - name: mocking
      enabled: true
      protocols: [http, https]
      config:
        api_specification: |+
          openapi: 3.0.3
          info:
            title: Balance API
            version: "1.0.0"
          paths:
            /balance:
              get:
                responses:
                  "200":
                    description: Balance
                    content:
                      application/json:
                        schema:
                          type: object
                          properties:
                            accountId: { type: string }
                            availableBalance: { type: number }
                            currency: { type: string }

  # -------- Route 5: /transactions --------
  - name: transactions-route
    tags: [managed-by-deck]
    paths: [/transactions]
    methods: [GET]
    protocols: [https]
    strip_path: true
    preserve_host: false
    plugins:
    - name: mocking
      enabled: true
      protocols: [http, https]
      config:
        api_specification: |+
          openapi: 3.0.3
          info:
            title: Transactions API
            version: "1.0.0"
          paths:
            /transactions:
              get:
                responses:
                  "200":
                    description: Transaction list
                    content:
                      application/json:
                        schema:
                          type: array
                          items:
                            type: object
                            properties:
                              txnId: { type: string }
                              amount: { type: number }
                              txnDate: { type: string, format: date-time }
                              status: { type: string }

  # -------- Route 6: /payments --------
  - name: payments-route
    tags: [managed-by-deck]
    paths: [/payments]
    methods: [POST]
    protocols: [https]
    strip_path: true
    preserve_host: false
    plugins:
    - name: mocking
      enabled: true
      protocols: [http, https]
      config:
        api_specification: |+
          openapi: 3.0.3
          info:
            title: Payments API
            version: "1.0.0"
          paths:
            /payments:
              post:
                responses:
                  "201":
                    description: Payment created
                    content:
                      application/json:
                        schema:
                          type: object
                          properties:
                            paymentId: { type: string }
                            status: { type: string }
                            message: { type: string }

  # -------- Route 7: /cards --------
  - name: cards-route
    tags: [managed-by-deck]
    paths: [/cards]
    methods: [GET]
    protocols: [https]
    strip_path: true
    preserve_host: false
    plugins:
    - name: mocking
      enabled: true
      protocols: [http, https]
      config:
        api_specification: |+
          openapi: 3.0.3
          info:
            title: Cards API
            version: "1.0.0"
          paths:
            /cards:
              get:
                responses:
                  "200":
                    description: Cards list
                    content:
                      application/json:
                        schema:
                          type: array
                          items:
                            type: object
                            properties:
                              cardNumber: { type: string }
                              type: { type: string }
                              status: { type: string }

  # -------- Route 8: /loans --------
  - name: loans-route
    tags: [managed-by-deck]
    paths: [/loans]
    methods: [GET]
    protocols: [https]
    strip_path: true
    preserve_host: false
    plugins:
    - name: mocking
      enabled: true
      protocols: [http, https]
      config:
        api_specification: |+
          openapi: 3.0.3
          info:
            title: Loans API
            version: "1.0.0"
          paths:
            /loans:
              get:
                responses:
                  "200":
                    description: Loans list
                    content:
                      application/json:
                        schema:
                          type: array
                          items:
                            type: object
                            properties:
                              loanId: { type: string }
                              principal: { type: number }
                              status: { type: string }

  # -------- Route 9: /auth --------
  - name: auth-route
    tags: [managed-by-deck]
    paths: [/auth]
    methods: [POST]
    protocols: [https]
    strip_path: true
    preserve_host: false
    plugins:
    - name: mocking
      enabled: true
      protocols: [http, https]
      config:
        api_specification: |+
          openapi: 3.0.3
          info:
            title: Auth API
            version: "1.0.0"
          paths:
            /auth:
              post:
                responses:
                  "200":
                    description: Auth token
                    content:
                      application/json:
                        schema:
                          type: object
                          properties:
                            accessToken: { type: string }
                            expiresIn: { type: number }

  # -------- Route 10: /profile --------
  - name: profile-route
    tags: [managed-by-deck]
    paths: [/profile]
    methods: [GET]
    protocols: [https]
    strip_path: true
    preserve_host: false
    plugins:
    - name: mocking
      enabled: true
      protocols: [http, https]
      config:
        api_specification: |+
          openapi: 3.0.3
          info:
            title: Profile API
            version: "1.0.0"
          paths:
            /profile:
              get:
                responses:
                  "200":
                    description: User profile
                    content:
                      application/json:
                        schema:
                          type: object
                          properties:
                            name: { type: string }
                            email: { type: string }
                            mobile: { type: string }

  # -------- Route 11: /kyc --------
  - name: kyc-route
    tags: [managed-by-deck]
    paths: [/kyc]
    methods: [GET]
    protocols: [https]
    strip_path: true
    preserve_host: false
    plugins:
    - name: mocking
      enabled: true
      protocols: [http, https]
      config:
        api_specification: |+
          openapi: 3.0.3
          info:
            title: KYC API
            version: "1.0.0"
          paths:
            /kyc:
              get:
                responses:
                  "200":
                    description: KYC status
                    content:
                      application/json:
                        schema:
                          type: object
                          properties:
                            kycStatus: { type: string }
                            lastUpdated: { type: string, format: date }

  # -------- Route 12: /limits --------
  - name: limits-route
    tags: [managed-by-deck]
    paths: [/limits]
    methods: [GET]
    protocols: [https]
    strip_path: true
    preserve_host: false
    plugins:
    - name: mocking
      enabled: true
      protocols: [http, https]
      config:
        api_specification: |+
          openapi: 3.0.3
          info:
            title: Limits API
            version: "1.0.0"
          paths:
            /limits:
              get:
                responses:
                  "200":
                    description: Limits
                    content:
                      application/json:
                        schema:
                          type: object
                          properties:
                            dailyLimit: { type: number }
                            usedLimit: { type: number }

  # -------- Route 13: /beneficiaries --------
  - name: beneficiaries-route
    tags: [managed-by-deck]
    paths: [/beneficiaries]
    methods: [GET]
    protocols: [https]
    strip_path: true
    preserve_host: false
    plugins:
    - name: mocking
      enabled: true
      protocols: [http, https]
      config:
        api_specification: |+
          openapi: 3.0.3
          info:
            title: Beneficiaries API
            version: "1.0.0"
          paths:
            /beneficiaries:
              get:
                responses:
                  "200":
                    description: Beneficiaries list
                    content:
                      application/json:
                        schema:
                          type: array
                          items:
                            type: object
                            properties:
                              name: { type: string }
                              account: { type: string }
                              bank: { type: string }

  # -------- Route 14: /notifications --------
  - name: notifications-route
    tags: [managed-by-deck]
    paths: [/notifications]
    methods: [GET]
    protocols: [https]
    strip_path: true
    preserve_host: false
    plugins:
    - name: mocking
      enabled: true
      protocols: [http, https]
      config:
        api_specification: |+
          openapi: 3.0.3
          info:
            title: Notifications API
            version: "1.0.0"
          paths:
            /notifications:
              get:
                responses:
                  "200":
                    description: Notifications
                    content:
                      application/json:
                        schema:
                          type: array
                          items:
                            type: object
                            properties:
                              id: { type: string }
                              message: { type: string }
                              read: { type: boolean }

  # -------- Route 15: /health --------
  - name: health-route
    tags: [managed-by-deck]
    paths: [/health]
    methods: [GET]
    protocols: [https]
    strip_path: true
    preserve_host: false
    plugins:
    - name: mocking
      enabled: true
      protocols: [http, https]
      config:
        api_specification: |+
          openapi: 3.0.3
          info:
            title: Health API
            version: "1.0.0"
          paths:
            /health:
              get:
                responses:
                  "200":
                    description: Health status
                    content:
                      application/json:
                        schema:
                          type: object
                          properties:
                            status: { type: string }
                            timestamp: { type: string, format: date-time }
